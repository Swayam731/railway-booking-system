<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Passenger Details</title>
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.3.3/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #e3f2fd; /* Light blue background for a softer look */
      font-family: Arial, sans-serif;
    }
    .container {
      width: 100%;
      max-width: 600px; /* Responsive width */
      background-color: #ffffff; /* White background for the form */
      border: 2px solid #007bff; /* Bootstrap blue border */
      border-radius: 10px; /* Rounded corners */
      margin: 50px auto; /* Centering with margin */
      padding: 20px; /* Padding inside the container */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
    }
    .heading {
      text-align: center;
      font-weight: bold;
      color: #007bff; /* Bootstrap blue for headings */
      margin-bottom: 20px;
    }
    table {
      width: 100%;
      margin: 0 auto;
    }
    table td {
      padding: 10px; /* Spacing inside cells */
      vertical-align: middle; /* Align items vertically in the middle */
    }
    .form-control, .form-select {
      border-radius: 5px; /* Rounded corners for input fields */
      box-shadow: inset 0 1px 2px rgba(0, 0, 0, 0.1); /* Subtle inner shadow */
    }
    .btn-primary {
      background-color: #007bff; /* Bootstrap blue button */
      border-color: #007bff;
      border-radius: 5px; /* Rounded corners */
      padding: 10px 20px; /* Padding inside the button */
    }
    .btn-primary:hover {
      background-color: #0056b3; /* Darker blue on hover */
      border-color: #0056b3;
    }
    .error_messages {
      background-color: #f8d7da; /* Light red background for error messages */
      color: #721c24; /* Dark red color for text */
      border: 1px solid #f5c6cb; /* Red border */
      border-radius: 5px; /* Rounded corners */
      padding: 15px; /* Padding inside the error message box */
      margin-bottom: 20px; /* Space below the error message box */
    }
    .error_message {
      color: #dc3545; /* Red color for individual error messages */
      font-size: 14px; /* Slightly smaller font size */
    }
  </style>
</head>
<body>

  <div class="container">
    <%= form_with model: @passenger do |form| %>
      <% if @passenger.errors.any? %>
        <div class="error_messages">
          <h3>There were some errors with your submission:</h3>
          <ul>
            <% @passenger.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      
      <h2 class="heading">Enter Your Details Here</h2>
      <%= form.hidden_field :ticket_id, value: @ticket_id %>
      <table>
        <tr>
          <td colspan="3" class="text-center"><strong>Passenger 1</strong></td>
        </tr>
        <tr>
          <td><%= form.label :name, "Name" %></td>
          <td><%= form.text_field :name, class: "form-control" %></td>
          <td class="error_message"><%= @passenger.errors[:name].first %></td>
        </tr>
        <tr>
          <td><%= form.label :age, "Age" %></td>
          <td><%= form.number_field :age, class: "form-control" %></td>
          <td class="error_message"><%= @passenger.errors[:age].first %></td>
        </tr>
        <% if @number_of_passenger == 2 %>
          <tr>
            <td colspan="3" class="text-center"><strong>Passenger 2</strong></td>
          </tr>
          <tr>
            <td><%= form.label :name2, "Name" %></td>
            <td><%= form.text_field :name2, class: "form-control" %></td>
            <td class="error_message"><%= @passenger.errors[:name2].first %></td>
          </tr>
          <tr>
            <td><%= form.label :age2, "Age" %></td>
            <td><%= form.number_field :age2, class: "form-control" %></td>
            <td class="error_message"><%= @passenger.errors[:age2].first %></td>
          </tr>
        <% end %>
        <tr>
          <td colspan="3" class="text-center">
            <%= form.submit "Proceed", class: "btn btn-primary" %>
          </td>
        </tr>
      </table>
    <% end %>
  </div>

  <!-- Bootstrap JS Bundle with Popper -->
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.3.3/js/bootstrap.bundle.min.js"></script>
</body>
</html>
