<%= form_tag payments_path do %>
  <label class="amount">
      <span>Amount: ₹<%= @ticket.fare%></span>
  </label>

  <script
    src="https://checkout.stripe.com/checkout.js" class="stripe-button"
    data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
    data-description="Pay for book your ticket"
    data-amount= <%= @ticket.fare%>
    data-locale="auto">
  </script>
<% end %>